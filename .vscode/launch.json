{
  "configurations": [
    {
      "name": "(gdb) Launch Server",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/server",
      "args": [
        "5000"
      ],
      "cwd": "${workspaceFolder}",
    }
  ],
  "version": "2.0.0"
}