{
  "configurations": [
    {
      "name": "Launch Client",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/build/client",
      "args": ["localhost", "5000"],
      "cwd": "${workspaceFolder}",
    },
    {
      "name": "Launch Server erwin",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/build/server",
      "args": ["erwin"],
      "cwd": "${workspaceFolder}",
    },
    {
      "name": "Launch Server levi",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/build/server",
      "args": ["levi"],
      "cwd": "${workspaceFolder}",
    },
    {
      "name": "Test",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/build/test",
      "args": ["${input:lldbArgs}"],
      "cwd": "${workspaceFolder}",
    },
  ],
  "inputs": [
    {
      "id": "lldbArgs",
      "type":"promptString",
      "description": "args for lldb",
      "default": ""
    },
  ],
  "version": "2.0.0"
}